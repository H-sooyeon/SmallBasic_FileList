NEWLINE = Text.GetCharacter(13) + Text.GetCharacter(10)



single_line = "bla bla bla..."



TextWindow.Write(single_line)

TextWindow.Write("(on the same line)")

TextWindow.WriteLine("... WriteLine adds a NEWLINE")



TextWindow.WriteLine("---------------------------------")

multiple_lines = single_line + NEWLINE + "second line..." + NEWLINE



TextWindow.Write(multiple_lines)


