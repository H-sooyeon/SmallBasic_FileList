'Need to run as admistrator, requires Data.dll V.0.9.0.2.
Security.RunAsAdmistrator()
Command = "C:\Program Files\Microsoft\Small Basic\SmallBasicCompiler.exe C:\Compilieren.sb"
X = Process.Start(command)
If X = -1 Then
  TextWindow.WriteLine("Failed")
Else
  TextWindow.WriteLine("Success")
EndIf
TextWindow.Read()
Program.End()
  
