TextWindow.WriteLine("This is Math.GetRandomNumber method ........")
textwindow.WriteLine(Clock.Date+" : "+Clock.Time+ "...  "+Clock.ElapsedMilliseconds)
For m=1 To 10
  randam=""
  N=0
  While N<2000
    N=N+1
    rnd=math.GetRandomNumber(10)-1
    randam[rnd]=randam[rnd]+1
    sum[rnd]=sum[rnd]+1
  EndWhile
  For i=0 To 9
    TextWindow.Write(i+"="+randam[i]+";")
  EndFor
  TextWindow.WriteLine(" ")
EndFor
TextWindow.WriteLine(" ")
For i=0 To 9
  TextWindow.Write(i+"="+sum[i]+";")
EndFor
TextWindow.WriteLine(" ")

