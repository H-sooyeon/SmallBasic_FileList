' The following line could be harmful and has been automatically commented.

' AC = LDFile.GetAllDirectories("C:/")

' The following line could be harmful and has been automatically commented.

' AE = LDFile.GetAllDirectories("E:/")



IAC = Array.GetItemCount(AC)

For i = 1 To IAC

  TextWindow.WriteLine(AC[i])

EndFor 



IAE = Array.GetItemCount(AC)

For i = 1 To IAE

  TextWindow.WriteLine(AC[i])

EndFor 










