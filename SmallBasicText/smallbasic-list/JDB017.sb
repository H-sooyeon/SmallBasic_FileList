sent= "ecnetnes a ni sretcarahc eht lla esrever ot margorp a etirW"
TextWindow.WriteLine(sent)
TextWindow.WriteLine(" -> Converted to -> ")
len = Text.GetLength(sent)
    'reverse=""
For i = 0 to len
  char = Text.GetSubText(sent,len-i,1)
  TextWindow.Write(char)
    ' reverse=reverse+char
EndFor
    'TextWindow.WriteLine(reverse)
TextWindow.WriteLine("")
