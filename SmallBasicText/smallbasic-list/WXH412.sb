'Age Checker for Monthly Challenge for march
'Use TextWidnow!
TextWindow.Title = "Age Checker"
begin:
TextWindow.WriteLine ("Enter your age: ")
age = TextWindow.Read()
TextWindow.WriteLine ("Enter your Birth Year: ")
year = TextWindow.Read()
number = ( Clock.Year - year)
If (number > age) Then
  TextWindow.Write("False.")
Endif
If (number < age) Then 
  TextWindow.Write("False.")
EndIf
If (number = age) Then
  TextWindow.WriteLine("True.")
Endif
Goto begin
  
