TextWindow.WriteLine("--------------------")
curNum = 1
Total = 0
While curNum < 1000
  search = Text.IsSubText(curNum, 3)
  If search = "True" Then
    TextWindow.WriteLine(curNum)
  EndIf
  Total = Total + curNum
  curNum = curNum + 1
EndWhile
TextWindow.WriteLine("--------------------")
  TextWindow.WriteLine("Total Is: " + Total)
