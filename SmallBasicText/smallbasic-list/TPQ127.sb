' Just a simple 0 to 255 hex converter in 3 lines

TextWindow.WriteLine("enter the value you wish to convert:")
number = TextWindow.Read()

TextWindow.WriteLine( Text.Append(text.GetSubText("0123456789ABCDEF", Math.Floor(number / 16) + 1,1), text.GetSubText("0123456789ABCDEF",((number / 16 - Math.Floor(number / 16)) * 16) +1,1)))
